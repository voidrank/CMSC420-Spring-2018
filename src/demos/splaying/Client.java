package demos.splaying;

import java.util.Random;

/**
 * <p><tt>Client</tt> is a client program that attempts to empirically determine the amortized complexity order
 * of splay trees, <em>O(m logn)</em> for <em>m</em> sequential insertions, deletions or searches, and <em>n</em>
 * the largest amount of nodes ever present in the tree during those <em>m</em> sequential runs.</p>
 * @author Jason
 * @see SplayTree
 */
public class Client {
    public static void main(String[] args){
        Random r = new Random();

    }
}
